node_modules/
.expo/
dist/
*.log

# Generated by `expo prebuild` from app.json and the config plugin. Both are
# built on demand — in CI and on EAS — and committing either would let a
# hand-edit survive that the next prebuild would silently discard.
android/
ios/
