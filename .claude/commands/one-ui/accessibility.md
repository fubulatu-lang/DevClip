---
description: "Audit or apply One UI accessibility: screen reader, focus order, contrast, 200% text, targets"
argument-hint: "<file, dir or screen>"
---

Use the `one-ui-accessibility` skill.

Target: $ARGUMENTS

If no target is given, work on the current repository root.

Before starting, read `.claude/one-ui/reference/PLATFORMS.md` to detect the platform and
`.claude/one-ui/reference/TOKENS.md` for the token values. Do not guess the platform — ask if
the markers are ambiguous.
